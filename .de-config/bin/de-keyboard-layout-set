#!/usr/bin/env sh

set -e

if [ "$1" != "br" ] && [ "$1" != "us" ]; then
   echo "Error: Use 'br' or 'us'" >&2
   exit 1
fi

hyprctl keyword input:kb_layout "$1"
echo "$1" > "$HOME/.de-config/session/keyboard_layout"
