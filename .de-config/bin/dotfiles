#!/usr/bin/env sh

# Track dotfiles directly with git.
# see: https://wiki.archlinux.org/title/Dotfiles
GIT_SSH_COMMAND="ssh -i ~/.ssh/std_ed25519" /usr/bin/git --git-dir="$HOME/.dotfiles/" --work-tree="$HOME" "$@"

